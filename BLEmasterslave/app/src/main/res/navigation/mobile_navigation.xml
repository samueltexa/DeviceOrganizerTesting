<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/mobile_navigation"
            app:startDestination="@+id/scanner">

    <fragment
            android:id="@+id/scanner"
            android:name="App.Interfaces.Scanner.ScannerFragment"
            android:label="@string/scanner"
            tools:layout="@layout/scannerlistview"/>

    <fragment
            android:id="@+id/advertiser"
            android:name="App.Interfaces.Advertiser.AdvertiserFragment"
            android:label="@string/advertiser"
            tools:layout="@layout/advertiser_fragment"/>
</navigation>